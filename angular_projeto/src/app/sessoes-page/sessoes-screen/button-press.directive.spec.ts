import { ButtonPressDirective } from './button-press.directive';

describe('ButtonPressDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPressDirective();
    expect(directive).toBeTruthy();
  });
});
